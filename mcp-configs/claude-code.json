{
  "mcpServers": {
    "windows-mcp": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--directory", "C:/codedev/windows-mcp", "windows-mcp"],
      "env": {
        "ANONYMIZED_TELEMETRY": "false"
      }
    },
    "windows-mcp-sse": {
      "type": "sse",
      "url": "http://localhost:8765/sse",
      "_note": "Start server first: uv run windows-mcp --transport sse --port 8765",
      "_auth_note": "With auth: uv run windows-mcp --transport sse --port 8765 --api-key <key>"
    }
  }
}
